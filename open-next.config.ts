import { defineCloudflareConfig } from "@opennextjs/cloudflare";

export default defineCloudflareConfig({
    /*
     * You can customize the Cloudflare adapter configuration here.
     * For typical deployments, the defaults are sufficient.
     */
});
