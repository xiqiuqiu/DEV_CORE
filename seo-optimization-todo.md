# SIGCLR SEO 优化 TODO 清单

## 完成标准

- 搜索引擎能发现首页、博客列表、每篇文章。
- 每个重要页面都有清楚、唯一的标题和描述。
- 不该被搜索收录的工具页不会进入搜索结果。
- 文章分享到社交平台时显示文章自己的标题和摘要。
- 页面结构更清楚，后续内容可以持续扩展。

## 阶段 1：修复收录基础

- [ ] 更新网站地图，把以下页面加入：`/`、`/blog`、所有 `/blog/...` 文章页。  
  验收：打开 `https://sigclr.com/sitemap.xml` 能看到博客列表和两篇文章地址。

- [ ] 网站地图里的更新时间改成真实内容更新时间，不要每次访问都变。  
  验收：连续刷新 `sitemap.xml`，时间不会无意义变化。

- [ ] 给首页、博客列表、每篇文章添加标准链接。  
  验收：每个页面都能明确指向自己的正式地址。

- [ ] 禁止 `/deploy` 和 `/p/[id]` 进入搜索结果。  
  验收：这类页面带有“不收录”的标记；首页、博客、文章页仍然允许收录。

- [ ] 检查 `robots.txt` 保持允许主站页面抓取，并继续指向网站地图。  
  验收：`robots.txt` 不阻止首页、博客、文章页。

## 阶段 2：修复页面主题表达

- [ ] 首页只保留一个真正的一级标题。视觉上的重复 “SIGCLR” 不再被搜索引擎当成标题。  
  验收：首页只有一个一级标题。

- [ ] 文章页只保留一个一级标题。正文里重复出现的文章标题降级或隐藏。  
  验收：每篇文章页只有一个一级标题。

- [ ] 优化首页标题和描述，减少关键词堆叠，突出核心定位。  
  建议方向：`SIGCLR 澄讯空间 | 产品型开发者与 AI 工具开发`。

- [ ] 优化博客列表标题，不再只显示 `Blog`。  
  建议方向：`技术博客 | SIGCLR 澄讯空间`。

- [ ] 每篇文章使用独立标题、摘要和分享信息。  
  验收：文章页分享到社交平台时，不再显示首页的通用标题。

## 阶段 3：修复语言和展示一致性

- [ ] 明确网站默认语言。如果当前主要面向中文用户，默认语言应改为中文。  
  验收：页面源代码和浏览器看到的语言一致。

- [ ] 修正社交平台语言标记。中文页面不要继续标成英文。  
  验收：首页和文章页的语言标记与内容一致。

- [ ] 如果未来要做中英文双语，拆成独立地址，而不是只靠浏览器切换。  
  验收：例如中文和英文页面各有自己的正式地址，并互相标注对应关系。

## 阶段 4：补充结构化信息

- [ ] 首页保留个人/品牌信息，并补充网站信息。  
  验收：搜索引擎能识别这是个人品牌/作品集网站。

- [ ] 博客列表补充博客结构信息。  
  验收：搜索引擎能识别 `/blog` 是文章列表页。

- [ ] 每篇文章补充文章结构信息：标题、作者、发布时间、更新时间、摘要。  
  验收：文章能通过结构化数据检测工具检查。

- [ ] 给博客文章加面包屑信息。  
  验收：搜索引擎能理解“首页 > 博客 > 文章”的层级。

## 阶段 5：扩展可排名内容

- [ ] 为每个项目创建独立详情页。  
  验收：每个项目都有自己的页面，而不是只在首页卡片里出现。

- [ ] 每个项目页包含：项目背景、解决了什么问题、核心功能、结果、截图或演示链接。  
  验收：页面不是薄内容，能独立回答用户为什么要看这个项目。

- [ ] 建立内部链接：首页链接到项目页，项目页链接到相关文章，文章页链接到相关项目。  
  验收：重要页面之间不是孤立的。

- [ ] 规划 3 个内容主题集群：`AI 工具开发`、`前端产品体验`、`部署与工程实践`。  
  验收：每个主题至少有 3 篇文章计划。

- [ ] 每篇新文章增加“相关文章”区域。  
  验收：用户读完一篇文章后能继续进入同主题内容。

## 阶段 6：修复体验和间接 SEO 风险

- [ ] 网易云音乐播放器改成用户点击后再加载。  
  验收：首页首屏不再主动加载大量第三方音乐资源。

- [ ] Google Analytics 延后加载，避免影响首屏体验。  
  验收：首页主要内容先显示，统计脚本后加载。

- [ ] 修复文字对比度不足的问题。  
  验收：Lighthouse 可访问性不再提示对比度问题。

- [ ] 给右侧终端输入框补充可识别名称。  
  验收：Lighthouse 不再提示输入框缺少标签。

- [ ] 优化字体加载造成的轻微页面移动。  
  验收：首页 CLS 继续保持低于 0.1。

## 阶段 7：上线后验证

- [ ] 重新运行构建。  
  验收：构建成功。

- [ ] 本地检查首页、博客列表、文章页、网站地图、robots。  
  验收：页面正常打开，关键信息正确。

- [ ] 用 Lighthouse 重新检查首页和文章页。  
  验收：SEO 保持 100，可访问性和最佳实践分数提升。

- [ ] 在 Google Search Console 提交网站地图。  
  验收：Search Console 能读取 sitemap。

- [ ] 用 URL 检查工具提交首页、博客列表、两篇文章。  
  验收：页面可抓取、可索引。

- [ ] 一周后检查收录情况和搜索展示。  
  验收：至少能看到 Search Console 中的抓取记录和页面状态。
