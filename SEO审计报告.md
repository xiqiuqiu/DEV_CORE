
## SEO审计报告
我检查了本地生产构建、线上站点、`robots.txt`、`sitemap.xml`、首页、博客列表、文章页，并跑了 Lighthouse 和页面性能追踪。结果是：基础 SEO 检查能过，Lighthouse SEO 是 100，但站点仍有明显的搜索增长短板。

**主要缺陷**

1. 网站地图只提交了首页，博客列表和两篇文章都没有提交。搜索引擎能发现它们，但发现效率和优先级会差很多。

2. 线上没有明显收录信号。我查了 `site:sigclr.com`，没有看到可用结果。这个只能作为外部信号，最终要以 Google Search Console 为准。

3. 首页和文章页都有重复的一级标题。首页视觉标题重复，文章页是页面标题和正文第一行标题重复。搜索引擎容易判断主题不够清晰。

4. 博客文章的社交分享信息仍然继承首页。文章页标题虽然正确，但分享卡片仍是 “SIGCLR – 澄讯空间”，不是文章标题和摘要。

5. 页面缺少标准链接声明。博客筛选页、文章页、首页都没有明确告诉搜索引擎“哪个 URL 是标准版本”，后续有参数页时容易分散权重。

6. 多语言处理不稳定。服务器初始语言是英文，但页面在浏览器里切成中文；Open Graph 语言也是英文。对中文内容为主的网站来说，这会让搜索引擎和社交平台收到混杂信号。

7. `/deploy` 和 `/p/[id]` 这类工具页目前可被收录，而且使用首页的通用标题描述。除非你希望这些页面排名，否则应该避免它们进入搜索结果。

8. 结构化数据太薄。现在只有个人信息，没有博客文章、面包屑、站点信息等结构化数据，文章页拿不到更好的搜索展示机会。

9. 首页内容主要集中在单页锚点里，项目没有独立详情页。这样不利于覆盖“产品开发”“AI 工具开发”“前端开发案例”等更具体搜索词。

10. 性能和可访问性有间接 SEO 风险。LCP 约 1.6 秒、CLS 约 0.06，本地结果是好的；但网易云音乐播放器和 Google Tag Manager 带来较多第三方加载。Lighthouse 还发现对比度不足、终端输入框缺少标签。

**优化方案**

第一优先级：让搜索引擎正确发现和理解页面。

- 把首页、博客列表、所有文章加入网站地图。
- 不要让网站地图的更新时间每次访问都变化，改成真实更新时间。
- 给首页、博客列表、每篇文章加标准链接。
- 给 `/deploy`、`/p/[id]` 加不收录规则；`/api/html/...` 已经有不收录规则，可以保留。
- 修复重复一级标题：首页只保留一个真实标题，视觉影子不作为标题；文章页正文里的第一个标题降一级或不重复显示。

第二优先级：提升点击率和展示质量。

- 首页标题改得更聚焦，不要堆太多技术词。
- `/blog` 标题不要只叫 “Blog”，建议改成“技术博客 | SIGCLR”一类更清楚的表达。
- 每篇文章单独生成分享标题、分享摘要、分享图片、文章类型。
- 中文为主就把默认语言、社交语言、页面内容保持一致；如果要双语，建议做独立中文/英文地址，并互相标注。

第三优先级：补充搜索可读内容。

- 给每个项目做独立详情页：问题、解决方案、技术选择、上线结果、截图。
- 首页围绕 1-2 个核心定位写清楚，比如“产品型开发者”“AI 产品开发”“前端与全栈交付”。
- 博客形成主题集群：React 性能、AI 工具、产品开发、Cloudflare/部署实践。
- 每篇文章增加作者信息、更新时间、相关文章链接、FAQ。

第四优先级：补结构化数据和信任信号。

- 首页保留个人信息结构化数据，但补充站点信息。
- 博客列表加博客结构化数据。
- 文章页加文章结构化数据。
- 页面层级加面包屑结构化数据。
- 关于页或首页补更清楚的合作方式、项目案例、真实身份/社交链接。

第五优先级：降低加载和体验风险。

- 网易云音乐播放器建议改成点击后再加载，避免首屏直接拉第三方脚本。
- Google Analytics 可以延后加载，减少首屏影响。
- 修复文字对比度和输入框标签。
- 字体加载导致了少量页面位移，可以通过更稳定的字体回退和尺寸设置继续压低。

**验证结果**

- `npm run build` 通过。
- 本地生产页面 `/`、`/blog`、两篇文章、`/sitemap.xml`、`/robots.txt` 都能正常访问。
- 线上对应页面也能正常访问。
- Lighthouse 桌面端：SEO 100，可访问性 90，最佳实践 77。
- 本地性能追踪：首页 LCP 约 1.6 秒，CLS 约 0.06。

参考检查入口：[首页](https://sigclr.com/)、[robots.txt](https://sigclr.com/robots.txt)、[sitemap.xml](https://sigclr.com/sitemap.xml)、[site:sigclr.com 检索](https://www.google.com/search?q=site%3Asigclr.com)。
